# 三机型专属配置与本次默认值

依据 iStoreOS 固定提交的板级 DTS 以及 [HINLINK 硬件手册](https://www.hinlink.cn/wp-content/uploads/2024/03/20240604094845067636.pdf) 调整。用户指定 2023—2024 年款及 MT7921E/MT7916；H66K 子版本差异已按厂商资料核对，详见 [H66K硬件核对](H66K硬件核对.md)。

| 项目 | H66K | H68K | H69K |
|---|---|---|---|
| 片上 GMAC | 两路均关闭 | 两路启用 | gmac1 启用、gmac0 关闭 |
| PCIe 2.5G | r8125 | r8125 | r8125 |
| 默认 LAN/WAN | eth1（检测到第三个 RTL8125 时加 eth2）/ eth0 | eth1 eth2 eth3 / eth0 | eth1 eth2 / eth0 |
| 板级 PWM 风扇 | 当前 DTS 未启用，不默认装模块 | 当前 DTS 无 fan 节点，不默认装模块 | 保留 pwm-fan 与温控模块 |
| 板载蜂窝软件 | 关闭 QMI/MBIM/ModemManager 等专用组件 | 关闭 QMI/MBIM/ModemManager 等专用组件 | 保留蜂窝组件及板级复位供电节点 |
| LCD 软件请求 | i2c5 关闭，因此关闭 lcdsimple | 保留原请求，实际屏幕待确认 | 保留原请求，实际屏幕待确认 |
| AP 默认名称 | H66K | H68K | H69K |
| AP 默认密码 | 无 | 无 | 无 |

`configs/devices/*.config` 是人工维护的专属差异；`configs/h66k.config` 等是生成的完整配置。Docker、文件共享、USB 存储等用户原有应用继续保留。移除没有板级依据的 r8126（5GbE）及 r8168（PCIe 1GbE）默认请求；片上 GMAC 使用 SoC 驱动，不依赖 r8168。

H66K MINI/PRO 为两个 2.5G 口、可扩展 PCIe Wi-Fi；MAX 为三个 2.5G 口且无该 Wi-Fi 扩展。H66K 初始化脚本检测到第三个 RTL8125 时将其加入 LAN，保留两口版本的默认分配。实际端口编号和驱动枚举时序仍需实机验证。无线保留用户指定的 MT7921E/MT7916 支持及原有 MT7922 兼容请求，不据此宣称 MAX 自带无线。

## NAT 和 BBR3

- 使用 ImmortalWrt 自带 `firewall4`、`kmod-nft-fullcone`，设置 `fullcone=1`、`fullcone6=0`。
- 设置 `flow_offloading=1`，开启软件分载；`flow_offloading_hw=0`。这里的“高性能”具体指软件分载，不承诺尚未测量的吞吐提升或硬件 NAT。
- 全锥实现主要作用于 UDP；保留 WAN 入站防火墙，不把 INPUT/FORWARD 改成全放行。来自上游运营商的 NAT 类型也不会因此自动改变。
- BBR3 补丁来源、版本与校验见 [BBR3来源](patch/BBR3来源.md)。包仍叫 `kmod-tcp-bbr`，算法仍叫 `bbr`，不能凭 sysctl 输出 bbr 判断版本；构建会检查源码中的版本 3。
- fq 位于当前源码的 `kmod-sched` 包中。默认 qdisc 为 fq，连接数仍为 655550。
- BBR控制本机发送端 TCP（例如本机代理、上传服务），不会直接替换内网客户端纯 NAT 转发连接的拥塞算法。软件分载可能影响流量统计、SQM/限速或部分代理策略，需要实测组合行为。

## GPU 要不要开

这套路由/NAT/代理/Docker/NAS 文件共享用途默认关闭 GPU 的设备树节点，不选择 Panfrost/Bifrost 包。GPU 不用于本工程的 NAT 或 TCP 拥塞控制；开启它不能让上述路径自动使用 GPU。

需要图形桌面、OpenGL 或明确支持 GPU 的计算程序时再开启，并配套用户态驱动。视频硬件转码属于 VPU 的编码/解码路径，与 GPU 是不同模块；还需相应内核驱动、FFmpeg/应用支持和实际格式测试。此工程没有完整移植 iStoreOS 厂商 VPU/NPU 加速栈。

## 实机验证

```sh
uci show wireless
uci show firewall | grep -E 'fullcone|flow_offloading'
fw4 print | grep fullcone
sysctl net.netfilter.nf_conntrack_max
sysctl net.core.default_qdisc net.ipv4.tcp_congestion_control
cat /sys/module/tcp_bbr/version
```

期望无线名称对应机型且 `encryption=none`；BBR模块版本为 `3`。实际 nft 规则、网卡 AP 能力、三款风扇/蜂窝功能及吞吐仍须刷机验证。
