# 验证记录（2026-09-23）

本文件区分已经执行的检查和仍未完成的硬件/编译验证。

## 已执行并通过

- GitHub API 获取并核对两个仓库的目标、设备树、镜像定义、U-Boot 配置、网口初始化及无线驱动依赖。
- ImmortalWrt 核对提交：`1e93f179100b5f60659109c518a9353452e0c271`，Rockchip 内核系列为 6.18，U-Boot 为 2026.07。
- iStoreOS 核对提交：`83cda09f2609f18be819909c4e632e6c85cef3ec`，Rockchip 内核系列为 6.12，U-Boot 为 2025.10。
- 原 iStoreOS 三合一 profile 为 `hinlink_opc-h6xk`；已拆成三个独立 profile，并将 H68K 的 model 名从 H68K/H69K 改成 H68K。
- 在下载的真实 ImmortalWrt 镜像与 U-Boot Makefile 快照上运行适配脚本，两次执行均成功且未重复插入。
- 三款配置分别生成，仅选中当前机型。
- Python 语法检查、Bash `-n` 检查、YAML 结构解析，以及工作流内嵌 shell 的语法检查。
- 6 项单元测试通过：配置拆分、幂等性、文件冲突保护、必要无线配置强制校验、可选配置漂移检查、重复软件包提供者检查。
- 以 Linux v6.18 的 `rk3568.dtsi`、`rk356x-base.dtsi`、`rk3568-pinctrl.dtsi`、`rockchip-pinconf.dtsi` 进行标签集合静态核对：移植设备树引用的标签均可找到定义。这不是 cpp/dtc 编译或 DT schema 校验。
- 产物收集脚本使用临时伪造 gzip 测试件验证：合法 gzip 可整理，损坏 gzip 返回失败。这些测试件不包含在交付包中，不是固件。
- 文件采用 UTF-8、LF 换行；压缩包包含隐藏目录 `.github`，不含测试缓存、源码下载缓存或伪造镜像。

## 尚未执行，不能宣称通过

- 完整 feeds 下载、安装、`make defconfig` 后的每个配置项满足性验证。
- Linux 内核、设备树、U-Boot、全部插件及根文件系统的真实编译。
- U-Boot 2025.10 配置移到 2026.07 后的运行验证、ATF/TPL 启动链验证。
- 镜像是否实际装得下 512 MiB rootfs、64 MiB 启动分区。
- H66K/H68K/H69K 各自的 SD/eMMC 启动、网口物理顺序、USB、无线 AP、风扇、重启及升级验证。
- Actions 完整运行尚未执行；目标仓库为 https://github.com/googlewmb/immortalwrt-hinlink 。

当前工程因此标记为“待 CI 和实机验证”，而不是“每次自动成功”。原始配置有大量跨版本选项，严格审计可能先停止；应根据实际 `config-drift.txt` 修复或明确接受非核心配置差异。构建失败不会自动删插件或发布镜像。

## 本轮配置调整

增加机型专属硬件片段；无线设为机型名称且开放；增加 firewall4 全锥 NAT、软件分载及固定 BBRv3 补丁；GPU 默认关闭。BBRv3 已在 Linux 6.18.52 原始源码上通过补丁应用检查，但完整 ImmortalWrt 补丁队列、全编译和实机验证仍待执行。具体子型号、年份和网卡仍待用户确认。

本轮 8 项配置/保护测试通过；另外验证 BBRv3 补丁安装幂等性、SHA256 校验、补丁应用后真实 tcp_bbr.c 的版本检查，以及拒绝 6.19 / 拒绝 BBRv1 的行为。Shell/Python 语法检查通过。

## H66K 子版本资料复核

厂商手册确认 MINI/PRO 两口、MAX 三口且无 PCIe 无线扩展。已调整 H66K 初始化脚本，仅将经 PCI ID 验证的第三个 RTL8125 加入 LAN。执行实际 shell 的四组模拟：两口、三口、USB/非 PCI、无线设备；全部通过。此测试不替代 MAX 实机 PCIe 枚举、启动和物理端口验证。详见 H66K硬件核对.md。

## 编译错误修复与审计（后续记录优先）

实际读取 Actions 35764953947 失败日志，定位 `kmod-oaf` 重复提供者引起的 Kconfig 递归依赖。修复后，Actions 35769353097 已完成 feeds 与 defconfig，不再出现该冲突，但严格配置审计列出旧配置缺失项。经用户明确选择，采用单独兼容排除清单，保留原始配置，补齐 NATMapT 来源并修正包名和依赖；完整差异见 `编译修复审计.md`。

当前 16 项回归测试通过；包括插件安装优先级、失败恢复、gzip 尾部元数据处理、错误机型/损坏镜像拒绝、单一机型及认证组件冲突保护。Python/Shell/YAML/工作流内嵌 Shell 语法检查通过。测试中的 fwtool 交互使用模拟，不代表完整镜像已生成。已启动 Actions 35770672021 进行三机型严格回归，完整编译与实机验证尚未宣称通过。
