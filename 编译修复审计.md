# 编译修复和逻辑审计

## 已确认的失败原因

原始失败运行：35764953947。失败发生于 `make defconfig` 和包冲突审计，尚未开始固件编译。

`feeds install -a` 按源码目录去重，官方 `open-app-filter` 和第三方 `oaf` 源码目录不同却导出相同的 `kmod-oaf`，导致 Kconfig 自选择。仅把官方 feeds 放前面不能保证二进制包名不冲突。

修复：暂时仅开放官方 feeds 安装，再恢复完整 feeds，按已生成的二进制包元数据，仅补齐当前机型请求的缺失包和依赖。继续保留最终重复包审计，不用 `-f` 覆盖官方包。回归运行 35769353097 已确认该冲突消失。

## 配置兼容处理

用户已选择“先完成可编译版本，逐项记录未集成项目”。原始配置 `configs/requested.config` 保留，明确排除项单独放入 `configs/compat.config`，不是打开全局配置漂移开关。

| 项目 | 处理及影响 |
|---|---|
| NATMapT / LuCI / stuntman-client | 补齐 muink 作者仓库的三个固定提交，不替换其他官方包 |
| ntfsprogs | 改为当前官方 ntfs-3g-utils，仍提供 NTFS 工具 |
| LIBQMI_COLLECTION_BASIC | H69K 改为 FULL，满足固件中 fwupd ModemManager 插件的约束 |
| luci-app-istorex=m | 安装逻辑同时处理 y/m 请求；仍是单独安装包，不宣称预装到镜像 |
| libevent2-core=m → y | 由依赖提升到镜像内，视为满足请求，不再误报缺失 |
| IB_STANDALONE | 不生成 ImageBuilder，关闭无效子选项 |
| dkml | 未移植 iStoreOS 动态内核模块加载器，使用 ImmortalWrt 原生模块加载 |
| lcdsimple | 未移植屏幕应用，不宣称屏幕显示可用；该项目上游描述面向 iStoreOS/easepi-r1 |
| istoreos-intl | 未集成旧版 iStoreOS 国际版定制组件 |
| kmod-fs-binfmt-misc | 当前包源无此 recipe，未提供额外的跨架构可执行格式模块 |
| mergerfs | 未集成合并文件系统程序 |
| luci-app-mergerfs | 未集成对应管理界面 |
| luci-i18n-mergerfs-zh-cn | 随主程序缺失，不集成其翻译 |
| luci-lib-mac-vendor | 当前包源无对应 recipe，未集成 |
| luci-i18n-fan-zh-cn | 无对应翻译包；H69K PWM 风扇内核驱动仍保留 |
| luci-i18n-filetransfer-zh-cn | 不存在独立翻译包；filetransfer 主程序仍保留 |
| luci-i18n-oled-zh-cn | 原配置只有翻译请求、没有 OLED 主程序，不承诺屏幕支持 |
| kmod-video-dma-contig | 隐藏辅助模块由实际视频驱动选择，不再强选；不承诺视频加速 |
| kmod-video-dv-timings | 当前包源无对应 recipe，不集成 |
| shadow-expiry | 当前官方 shadow 不再生成该工具，不集成 |
| shadow-groupmems | 当前官方 shadow 不再生成该工具，不集成 |
| shadow-logoutd | 当前官方 shadow 不再生成该工具，不集成 |
| librpcsecgss | 当前官方 NFS/libtirpc 构建禁用 GSS；普通 NFS 保留，未集成 Kerberos/GSS 功能 |

## 其他逻辑修复

- 升级镜像为 gzip 加 fwtool 尾部元数据。先用构建出的官方 fwtool 验证 CRC、提取元数据和纯 gzip，再验证 gzip 完整性；原始交付文件不截断。
- 校验元数据中的机型，拒绝错误机型；配置展开后必须恰好选中一个预期机型。
- 明确要求 r8125、mt7915e、指定无线固件、fullcone、offload、BBR 和 fq 的包存在；拒绝禁用的 GPU/网卡驱动、冲突认证组件被依赖重新启用。
- 缺失项和冲突详情同时写入日志和报告，避免 CI 页面只有“失败”而没有原因。
- 将回归测试加入 CI。脚本测试不是完整固件编译或实机验证。

## 尚需验证

完整 Linux 编译、BBRv3 在完整补丁队列中的应用、三机型启动和物理网口映射仍须以实际运行结果为准。Windows 本地只进行了脚本/语法测试。

## 第三轮构建回归

运行 35770672021 已确认 kmod-oaf 冲突不再出现。严格审计发现 NATMapT 三个仓库的根目录 Makefile 未被 feeds 索引；现改为把固定提交的 checkout 链接到 package/h6xk-extra，再生成软件包元数据。H66K/H68K 同时禁用随蜂窝父包关闭而失效的六个子选项。新增根目录 recipe 可扫描、重复执行及拒绝覆盖检查，本地共 18 项测试通过。完整编译仍待后续运行验证，不能将这些测试视为固件编译成功。
